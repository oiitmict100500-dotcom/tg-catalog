# Решение: Backend уже работает на Vercel!

## Хорошая новость! 🎉

У вас уже есть backend на Vercel! Он работает через папку `api/` как Vercel Serverless Functions.

Это значит, что API доступен по адресу: `https://tg-catalog-one.vercel.app/api/`

## Что нужно сделать:

### Вариант 1: Удалить VITE_API_URL (рекомендуется)

Поскольку ваш backend работает на том же домене через `/api/`, вам **НЕ НУЖНО** устанавливать `VITE_API_URL`!

1. Откройте Vercel: https://vercel.com
2. Откройте проект `tg-catalog`
3. Перейдите в **Settings → Environment Variables**
4. Найдите переменную `VITE_API_URL`
5. **УДАЛИТЕ** ее полностью (нажмите на иконку корзины)
6. Сохраните

После этого приложение будет использовать относительные пути `/api/`, которые автоматически будут работать с вашими serverless функциями.

### Вариант 2: Установить правильный URL

Если вы хотите явно указать URL:

1. Откройте Vercel: https://vercel.com
2. Откройте проект `tg-catalog`
3. Перейдите в **Settings → Environment Variables**
4. Найдите или создайте `VITE_API_URL`
5. Установите значение: `https://tg-catalog-one.vercel.app`
   - ⚠️ БЕЗ `/api` в конце!
   - ⚠️ БЕЗ слеша `/` в конце!
6. Сохраните

Но лучше использовать Вариант 1 (удалить переменную), так как код автоматически будет использовать относительные пути.

---

## Проверка

После исправления:

1. Подождите 1-2 минуты (Vercel перезапустит сайт)
2. Обновите страницу сайта (F5)
3. Откройте консоль браузера (F12)
4. **НЕ должно быть** ошибки: "VITE_API_URL указывает на frontend домен!"
5. Кнопка Telegram должна заработать!

---

## Почему это работает?

Ваш проект настроен так:
- Frontend развернут на: `tg-catalog-one.vercel.app`
- API работает через: `tg-catalog-one.vercel.app/api/`
- В `vercel.json` настроены rewrites для `/api/` → serverless functions из папки `api/`

Поэтому когда вы не указываете `VITE_API_URL`, приложение использует относительные пути `/api/`, которые автоматически работают с вашими функциями!

---

## Настройка токена бота

Не забудьте также настроить токен бота в переменных окружения:

1. В том же проекте Vercel
2. **Settings → Environment Variables**
3. Добавьте:
   - **Name:** `TELEGRAM_BOT_TOKEN`
   - **Value:** `8527710832:AAHZdTNESfu2ZAMSTD0YEb9BhTTfQb0mCfM`
   - **Environment:** Production, Preview, Development
4. Сохраните

Токен будет доступен в ваших serverless функциях через `process.env.TELEGRAM_BOT_TOKEN`.


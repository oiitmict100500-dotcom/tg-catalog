# TG Catalog

Каталог Telegram-ресурсов с интеграцией Telegram-бота.

## 🚀 Простой запуск

### Дважды кликните на файл: `ЗАПУСК_С_ЛОГАМИ.bat`

Этот батник:
- ✅ Создаст все файлы настроек
- ✅ Установит зависимости
- ✅ Запустит все компоненты (Backend, Bot, Frontend)
- ✅ **Сохранит все логи в папку `logs\`**
- ✅ Проверит, что все порты работают

### Обычный запуск (без сохранения логов в файлы): `ЗАПУСК.bat`

### Остановить все процессы: `ОСТАНОВИТЬ_ВСЕ.bat`

## 🔧 Настройка Telegram бота

### 1. Получите токен бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot` для создания нового бота
3. Или `/token` для получения токена существующего бота
4. Скопируйте токен (например: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 2. Установите токен

**Откройте файл `telegram-bot\.env` и вставьте токен:**

```
BOT_TOKEN=ваш_токен_здесь
API_URL=http://localhost:3000/api
MODERATORS_CHAT_ID=
DAILY_SUBMISSION_LIMIT=3
```

⚠️ **Важно:** Замените `your-telegram-bot-token` на реальный токен!

### 3. Проверьте бота

Запустите `ПРОВЕРИТЬ_БОТА.bat` для диагностики проблем с ботом.

## 🔍 Диагностика

### Если что-то не работает:

1. **Сначала запустите `ОСТАНОВИТЬ_ВСЕ.bat`** - остановит все процессы
2. **Затем запустите `ЗАПУСК_С_ЛОГАМИ.bat`** заново
3. **Проверьте окна PowerShell** - должны быть открыты 3 окна:
   - TG Catalog - Backend (порт 3000)
   - TG Catalog - Bot
   - TG Catalog - Frontend (порт 3001)
4. **Проверьте логи** - откройте папку `logs\` или запустите `ПОСМОТРЕТЬ_ЛОГИ.bat`

### Если бот не работает:

1. **Запустите `ПРОВЕРИТЬ_БОТА.bat`** - проверит настройки и токен
2. **Проверьте окно "TG Catalog - Bot"** - там должны быть сообщения об успешном запуске
3. **Проверьте токен** - убедитесь, что он правильный в `telegram-bot\.env`

## 📋 Логи

Все логи сохраняются в папку `logs\`:
- `backend_[время].log` - логи Backend (ошибки, предупреждения)
- `bot_[время].log` - логи Telegram Bot
- `frontend_[время].log` - логи Frontend

**Чтобы посмотреть логи:**
- Дважды кликните на `ПОСМОТРЕТЬ_ЛОГИ.bat`
- Или откройте папку `logs\` и откройте нужный файл
- Или посмотрите в окнах PowerShell

## ⚙️ Настройка Backend

### Установите токен бота в Backend

**Откройте файл `backend\.env` и установите токен:**

```
TELEGRAM_BOT_TOKEN=ваш_токен_здесь
```

(Должен быть такой же, как в `telegram-bot\.env`)

### Запустите базу данных

Установите PostgreSQL и создайте базу:
```sql
CREATE DATABASE tg_catalog;
```

Или используйте Docker:
```bash
docker-compose up -d postgres
```

## 📍 Проверка

После запуска `ЗАПУСК_С_ЛОГАМИ.bat`:

- **Сайт**: http://localhost:3001
- **API**: http://localhost:3000/api/health
- **Бот**: отправьте `/start` в Telegram
- **Логи**: папка `logs\`

## 🔧 Порт и хост

- **Frontend**: http://localhost:3001 (хост: 0.0.0.0)
- **Backend**: http://localhost:3000 (API префикс: /api)

Если порты заняты, измените их в:
- `frontend/vite.config.ts` - порт Frontend
- `backend/.env` - PORT для Backend

## 📄 Лицензия

MIT

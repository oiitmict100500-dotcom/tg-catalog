# 🚀 Простой способ развернуть Backend (5 минут)

## Вариант: Railway (самый простой)

### Шаг 1: Зайдите на Railway
1. Откройте https://railway.app
2. Нажмите "Login" → выберите "Login with GitHub"
3. Разрешите доступ к вашему GitHub аккаунту

### Шаг 2: Создайте проект
1. Нажмите большую кнопку **"New Project"**
2. Выберите **"Deploy from GitHub repo"**
3. Найдите ваш репозиторий `tg-catalog` и нажмите на него

### Шаг 3: Настройте проект
1. Railway автоматически определит проект
2. В настройках проекта найдите **"Settings"** → **"Root Directory"**
3. Укажите: `backend`
4. Нажмите "Save"

### Шаг 4: Добавьте переменные окружения
1. В проекте найдите вкладку **"Variables"**
2. Нажмите **"New Variable"**
3. Добавьте следующие переменные (по одной):

```
FRONTEND_URL = https://oiitmict100500-dotcom-tg-catalog.vercel.app
TELEGRAM_BOT_TOKEN = ваш_токен_бота_из_BotFather
JWT_SECRET = любой_случайный_текст_для_безопасности
NODE_ENV = production
```

**Где взять TELEGRAM_BOT_TOKEN:**
- Откройте Telegram → @BotFather
- Отправьте `/mybots`
- Выберите `tg_catalog_bot`
- Нажмите "API Token"
- Скопируйте токен

### Шаг 5: Настройте базу данных (опционально)
Railway может автоматически создать PostgreSQL:
1. В проекте нажмите **"New"** → **"Database"** → **"PostgreSQL"**
2. Railway создаст базу автоматически
3. Скопируйте переменные из базы (Railway покажет их автоматически)
4. Добавьте их в Variables:
   - `DB_HOST`
   - `DB_PORT`
   - `DB_USERNAME`
   - `DB_PASSWORD`
   - `DB_DATABASE`

### Шаг 6: Дождитесь деплоя
1. Railway автоматически начнет деплой
2. Подождите 2-3 минуты
3. Когда деплой завершится, Railway покажет URL вашего backend
4. URL будет выглядеть так: `https://your-app.railway.app`

### Шаг 7: Обновите Vercel
1. Откройте Vercel Dashboard
2. Settings → Environment Variables
3. Найдите `VITE_API_URL`
4. Замените значение на URL из Railway (например: `https://your-app.railway.app`)
5. Сохраните
6. Vercel автоматически пересоберет проект

## Готово! 🎉

Теперь ваш сайт должен работать полностью!

## Если что-то не работает:

1. Проверьте логи в Railway (вкладка "Deployments" → выберите deployment → "View Logs")
2. Убедитесь, что все переменные окружения добавлены
3. Проверьте, что Root Directory установлен в `backend`

## Преимущества Railway:

- ✅ Бесплатный план (достаточно для начала)
- ✅ Автоматический деплой из GitHub
- ✅ Автоматические обновления при push в GitHub
- ✅ Простой интерфейс
- ✅ Встроенная база данных PostgreSQL


# Как получить временный публичный домен

## Вариант 1: ngrok (рекомендуется, самый простой)

### Быстрая установка:

1. **Скачайте ngrok:**
   - Перейдите на https://ngrok.com/download
   - Скачайте версию для Windows
   - Распакуйте `ngrok.exe` в любую папку (например, `C:\ngrok\`)

2. **Зарегистрируйтесь и получите токен:**
   - Откройте https://dashboard.ngrok.com/signup
   - Зарегистрируйтесь (можно через Google/GitHub)
   - Перейдите на https://dashboard.ngrok.com/get-started/your-authtoken
   - Скопируйте ваш authtoken

3. **Настройте ngrok:**
   ```powershell
   ngrok config add-authtoken ВАШ_TOKEN_ЗДЕСЬ
   ```

4. **Запустите ngrok:**
   ```powershell
   ngrok http 3001
   ```

5. **Скопируйте домен:**
   В консоли вы увидите:
   ```
   Forwarding   https://abc123-def456.ngrok-free.app -> http://localhost:3001
   ```
   
   Скопируйте домен: `abc123-def456.ngrok-free.app` (без `https://`)

6. **Настройте в BotFather:**
   - Откройте [@BotFather](https://t.me/BotFather)
   - Отправьте `/setdomain`
   - Выберите бота `tgcatalog_bot`
   - Вставьте домен: `abc123-def456.ngrok-free.app`

**Готово!** Теперь ваш сайт доступен по публичному адресу.

---

## Вариант 2: Cloudflare Tunnel (бесплатно, без регистрации)

1. **Скачайте cloudflared:**
   - Перейдите на https://github.com/cloudflare/cloudflared/releases
   - Скачайте `cloudflared-windows-amd64.exe`
   - Переименуйте в `cloudflared.exe`

2. **Запустите туннель:**
   ```powershell
   cloudflared tunnel --url http://localhost:3001
   ```

3. **Скопируйте домен** из вывода (например: `https://random-name.trycloudflare.com`)

4. **Настройте в BotFather** (без `https://`)

**Примечание:** Домен меняется при каждом запуске.

---

## Вариант 3: localtunnel (через npm)

1. **Установите:**
   ```powershell
   npm install -g localtunnel
   ```

2. **Запустите:**
   ```powershell
   lt --port 3001
   ```

3. **Скопируйте домен** из вывода

4. **Настройте в BotFather**

---

## Вариант 4: serveo.net (без установки)

1. **Откройте PowerShell и выполните:**
   ```powershell
   ssh -R 80:localhost:3001 serveo.net
   ```

2. **Скопируйте домен** из вывода

3. **Настройте в BotFather**

**Примечание:** Требуется SSH клиент (обычно уже есть в Windows 10+).

---

## Сравнение вариантов:

| Сервис | Регистрация | Стабильность | Простота |
|--------|-------------|--------------|----------|
| **ngrok** | ✅ Нужна | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **Cloudflare Tunnel** | ❌ Не нужна | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| **localtunnel** | ❌ Не нужна | ⭐⭐⭐ | ⭐⭐⭐ |
| **serveo** | ❌ Не нужна | ⭐⭐ | ⭐⭐⭐ |

---

## Важно:

- **Домен меняется** при каждом перезапуске (кроме платных версий ngrok)
- После получения нового домена нужно **обновить настройки в BotFather**
- Используйте домен **без `https://`** в BotFather
- Убедитесь, что ваш **frontend запущен на порту 3001** перед запуском туннеля

---

## Быстрый старт (ngrok):

Если у вас уже установлен ngrok:

```powershell
# 1. Запустите frontend (если еще не запущен)
cd frontend
npm run dev

# 2. В другом окне PowerShell запустите ngrok
ngrok http 3001

# 3. Скопируйте домен из вывода ngrok
# 4. Настройте в BotFather через /setdomain
```

Готово! 🚀



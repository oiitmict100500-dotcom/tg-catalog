# 🔍 Диагностика белого экрана

## Что было исправлено

1. ✅ Добавлен Error Boundary для перехвата ошибок React
2. ✅ Улучшена обработка ошибок в main.tsx
3. ✅ Исправлен vercel.json для правильной обработки статических файлов
4. ✅ Добавлено логирование для диагностики

## Как проверить

### 1. Откройте консоль браузера (F12)

Нажмите F12 и перейдите на вкладку **Console**. Там должны быть логи:
- `✅ App rendered successfully` - приложение загрузилось
- `🔧 API Base URL configured` или `🔧 Using relative API paths` - конфигурация API
- Любые ошибки будут показаны с префиксом `❌`

### 2. Проверьте вкладку Network

1. Откройте вкладку **Network** в консоли браузера
2. Обновите страницу (F5)
3. Проверьте, загружаются ли файлы:
   - `index.html` - должен быть статус 200
   - `index-*.js` - JavaScript файл должен загрузиться
   - `index-*.css` - CSS файл должен загрузиться

Если файлы не загружаются (404 или другие ошибки):
- Проверьте, что сборка прошла успешно на Vercel
- Проверьте, что `outputDirectory` в vercel.json правильный: `frontend/dist`

### 3. Проверьте вкладку Sources

1. Откройте вкладку **Sources** в консоли браузера
2. Найдите файл `main.tsx` или `App.tsx`
3. Проверьте, нет ли синтаксических ошибок

## Возможные причины белого экрана

### 1. Ошибка JavaScript
**Симптомы**: В консоли есть ошибки с префиксом `❌`

**Решение**: 
- Проверьте консоль браузера
- Ошибка должна быть показана на экране (благодаря Error Boundary)
- Если видите сообщение об ошибке, скопируйте его и проверьте код

### 2. Файлы не загружаются
**Симптомы**: В Network вкладке файлы возвращают 404

**Решение**:
- Проверьте vercel.json - `outputDirectory` должен быть `frontend/dist`
- Убедитесь, что сборка прошла успешно
- Проверьте, что в `frontend/dist` есть файлы после сборки

### 3. Проблема с путями
**Симптомы**: Файлы загружаются, но приложение не работает

**Решение**:
- Проверьте, что в `vite.config.ts` `base: '/'` установлен правильно
- Проверьте, что в vercel.json rewrites настроены правильно

### 4. Ошибка в коде компонента
**Симптомы**: Приложение начинает загружаться, но падает

**Решение**:
- Error Boundary должен показать ошибку на экране
- Проверьте консоль для деталей
- Проверьте компонент, который вызывает ошибку

## Быстрая проверка

1. Откройте сайт
2. Нажмите F12
3. Перейдите на вкладку Console
4. Обновите страницу (F5)
5. Проверьте логи:
   - ✅ Должны быть логи о загрузке приложения
   - ❌ Если есть ошибки, они будут показаны

## Если ничего не помогает

1. **Очистите кеш браузера**:
   - Ctrl+Shift+Delete (Windows) или Cmd+Shift+Delete (Mac)
   - Выберите "Кешированные изображения и файлы"
   - Нажмите "Очистить данные"

2. **Проверьте сборку на Vercel**:
   - Откройте Vercel Dashboard
   - Проверьте последний deployment
   - Убедитесь, что сборка прошла успешно
   - Проверьте логи сборки на наличие ошибок

3. **Проверьте локально**:
   ```bash
   cd frontend
   npm run build
   npm run preview
   ```
   Если локально работает, проблема в конфигурации Vercel

4. **Проверьте vercel.json**:
   - Убедитесь, что `outputDirectory` правильный
   - Убедитесь, что `rewrites` настроены правильно

## Контакты для помощи

Если проблема не решена:
1. Скопируйте все ошибки из консоли браузера
2. Скопируйте логи из Vercel Dashboard
3. Проверьте, что все файлы загружаются в Network вкладке


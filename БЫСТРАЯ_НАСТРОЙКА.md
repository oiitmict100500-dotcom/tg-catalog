# 🚀 Быстрая настройка для работы авторизации через Telegram

## ✅ Шаг 1: Разверните Backend

Backend нужно развернуть на отдельном хостинге. Рекомендуемые варианты:

### Railway (самый простой)
1. Зарегистрируйтесь на https://railway.app
2. Создайте новый проект
3. Подключите ваш GitHub репозиторий
4. Выберите папку `backend`
5. Railway автоматически определит, что это Node.js проект
6. Добавьте переменные окружения:
   - `TELEGRAM_BOT_TOKEN` - ваш токен бота
   - `FRONTEND_URL` - URL вашего frontend на Vercel
   - `DATABASE_URL` - Railway автоматически создаст PostgreSQL
   - `PORT` - обычно не нужен, Railway сам определяет

### Render
1. Зарегистрируйтесь на https://render.com
2. Создайте новый Web Service
3. Подключите GitHub репозиторий
4. Укажите:
   - Build Command: `cd backend && npm install && npm run build`
   - Start Command: `cd backend && npm run start:prod`
5. Добавьте те же переменные окружения

## ✅ Шаг 2: Настройте переменную окружения на Vercel

1. Откройте https://vercel.com/dashboard
2. Выберите ваш проект `tg-catalog`
3. Перейдите в **Settings** → **Environment Variables**
4. Нажмите **Add New**
5. Заполните:
   - **Name**: `VITE_API_URL`
   - **Value**: `https://ваш-backend-url.railway.app` (или другой хостинг)
   - **Environment**: выберите все (Production, Preview, Development)
6. Нажмите **Save**

⚠️ **Важно**: URL должен быть БЕЗ слеша в конце!

## ✅ Шаг 3: Пересоберите проект

После добавления переменной окружения:
- Сделайте новый commit (любое изменение)
- Или в Vercel Dashboard → Deployments → нажмите "Redeploy"

## ✅ Шаг 4: Настройте домен в BotFather

1. Откройте Telegram → [@BotFather](https://t.me/BotFather)
2. Отправьте `/setdomain`
3. Выберите вашего бота (`tg_catalog_bot`)
4. Введите домен **БЕЗ** `https://` и **БЕЗ** слеша:
   ```
   oiitmict100500-dotcom-tg-catalog.vercel.app
   ```

## ✅ Шаг 5: Проверьте работу

1. Откройте ваш сайт на Vercel
2. Откройте консоль браузера (F12)
3. Попробуйте авторизоваться через Telegram
4. В консоли должны быть логи:
   - `📤 Sending auth request to: /api/auth/telegram`
   - `📤 Full URL: https://ваш-backend-url.com/api/auth/telegram`
   - `✅ Auth successful`

## 🔍 Если не работает

### Проверьте консоль браузера (F12)
- Если видите ошибку "VITE_API_URL не установлен" → установите переменную в Vercel
- Если видите "Failed to fetch" → проверьте, что backend запущен и доступен
- Если видите "401 Unauthorized" → проверьте домен в BotFather и токен бота

### Проверьте backend
- Backend должен быть запущен и доступен
- Проверьте URL backend в браузере: `https://ваш-backend-url.com/api/categories`
- Должен вернуться JSON с категориями

### Проверьте переменные окружения
- В Vercel: `VITE_API_URL` должен быть установлен
- На backend хостинге: `TELEGRAM_BOT_TOKEN` должен быть установлен

## 📖 Подробная инструкция

См. файл `НАСТРОЙКА_VERCEL_PRODUCTION.md`


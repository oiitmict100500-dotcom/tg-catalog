# Диагностика проблемы с отображением ресурсов

## Шаг 1: Проверка создания ресурса

1. **Одобрите заявку в панели модерации**
2. **Проверьте логи в Vercel:**
   - Перейдите в **Functions** → `/api/moderation`
   - Найдите последние логи с метками:
     - `🔨 Creating resource from submission:` - начало создания
     - `✅ Resource created successfully:` - успешное создание
     - `❌ Error creating resource:` - ошибка создания

## Шаг 2: Проверка ресурсов в базе данных

1. **Откройте тестовый endpoint:**
   - `https://ваш-домен.vercel.app/api/resources/test`
   - Должны быть видны все ресурсы в базе данных
   - Проверьте, есть ли ваша группа в списке

2. **Что должно быть в ответе:**
   ```json
   {
     "success": true,
     "stats": {
       "total": 1,
       "groups": 1
     },
     "resources": [
       {
         "id": "resource-...",
         "title": "Тестовая группа",
         "categoryId": "2",
         "authorId": "690956621",
         ...
       }
     ]
   }
   ```

## Шаг 3: Проверка загрузки ресурсов по категории

1. **Проверьте логи в Vercel:**
   - **Functions** → `/api/resources`
   - Найдите логи с метками:
     - `🔍 Query resources:` - запрос ресурсов
     - `📋 Resources loaded:` - загруженные ресурсы

2. **Проверьте запрос:**
   - Откройте в браузере: `https://ваш-домен.vercel.app/api/resources?category=2`
   - Должен вернуться JSON с ресурсами группы

## Шаг 4: Проверка ресурсов пользователя

1. **Проверьте логи в Vercel:**
   - **Functions** → `/api/users/me/resources`
   - Найдите логи с метками:
     - `📋 User resources:` - ресурсы пользователя

2. **Проверьте запрос:**
   - Войдите на сайт
   - Откройте консоль браузера (F12)
   - Перейдите на страницу "Мои ресурсы"
   - Проверьте запрос к `/api/users/me/resources`
   - Проверьте ответ API

## Возможные проблемы и решения

### Проблема 1: Ресурс не создается

**Симптомы:**
- В логах `/api/moderation` есть ошибка
- В `/api/resources/test` нет ресурса

**Решение:**
- Проверьте логи на ошибки создания таблицы
- Убедитесь, что PostgreSQL подключен
- Проверьте, что переменная `DATABASE_URL` установлена

### Проблема 2: Ресурс создается, но не загружается

**Симптомы:**
- В `/api/resources/test` ресурс есть
- Но в `/api/resources?category=2` его нет

**Решение:**
- Проверьте, что `categoryId` правильный (для групп должно быть '2')
- Проверьте логи запроса в `/api/resources`

### Проблема 3: Ресурс не отображается в "Мои ресурсы"

**Симптомы:**
- В `/api/resources/test` ресурс есть
- Но в `/api/users/me/resources` его нет

**Решение:**
- Проверьте, что `authorId` совпадает
- Может быть проблема с типами (число vs строка)
- Проверьте логи в `/api/users/me/resources`

### Проблема 4: Ресурс не отображается на главной странице

**Симптомы:**
- В `/api/resources?category=2` ресурс есть
- Но на главной странице его нет

**Решение:**
- Проверьте консоль браузера на ошибки
- Проверьте, что категория выбрана правильно
- Обновите страницу (Ctrl+F5)

## Проверка authorId

Важно: `authorId` должен быть строкой везде!

- При создании ресурса: `String(submission.authorId)`
- При загрузке ресурсов пользователя: `String(user.id)`
- При сравнении: оба должны быть строками

## Логи для отправки

Если проблема не решается, соберите следующие логи:

1. Логи из `/api/moderation` при одобрении заявки
2. Ответ от `/api/resources/test`
3. Ответ от `/api/resources?category=2`
4. Ответ от `/api/users/me/resources` (с авторизацией)
5. Логи из консоли браузера (F12)


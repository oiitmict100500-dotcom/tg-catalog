# Проверка отображения одобренных ресурсов

## Места отображения ресурсов

### 1. Главная страница (`/`)
**Файл:** `frontend/src/pages/Home.tsx`
**API:** `/api/resources?category={categoryId}`
**Как работает:**
- Пользователь выбирает категорию (Каналы, Группы, Боты и т.д.)
- Загружаются все ресурсы из этой категории из таблицы `resources`
- Ресурсы отображаются в сетке (`resources-grid`)
- Показываются только одобренные ресурсы (все ресурсы в таблице `resources` считаются одобренными)

**Проверено:**
✅ API endpoint `/api/resources` правильно обрабатывает запросы
✅ Запрос передает `category` параметр
✅ Данные возвращаются в формате `{ resources: [...], total: ..., page: ..., totalPages: ... }`
✅ Фронтенд правильно обрабатывает ответ и отображает ресурсы
✅ Логирование добавлено для отладки

---

### 2. Мои ресурсы (`/my-resources`)
**Файл:** `frontend/src/pages/MyResources.tsx`
**API:** `/api/users/me/resources`
**Как работает:**
- Загружаются все ресурсы текущего пользователя из таблицы `resources`
- Фильтруются по `author_id` текущего пользователя
- Показываются все ресурсы пользователя (одобренные)
- Ресурсы помечаются как `isPublished: true`

**Проверено:**
✅ API endpoint `/api/users/me/resources` требует авторизацию (токен)
✅ Фронтенд правильно передает токен авторизации
✅ Запрос фильтрует ресурсы по `author_id`
✅ Данные возвращаются с правильной структурой (включая `category` и `isPublished`)
✅ Фронтенд правильно отображает ресурсы
✅ Добавлено логирование для отладки

---

### 3. Детальная страница ресурса (`/resource/:id`)
**Файл:** `frontend/src/pages/ResourceDetail.tsx`
**API:** `/api/resources?id={resourceId}`
**Как работает:**
- Загружается конкретный ресурс по ID из таблицы `resources`
- Отображается полная информация о ресурсе
- Админ может редактировать ресурс

**Проверено:**
✅ API endpoint `/api/resources?id=...` правильно обрабатывает запросы
✅ Возвращает полную информацию о ресурсе

---

## Процесс создания и отображения ресурса

### Шаг 1: Создание заявки
1. Пользователь заполняет форму → `POST /api/resources/submit`
2. Заявка сохраняется в таблицу `moderation_submissions` со статусом `pending`

### Шаг 2: Модерация
1. Админ загружает заявки → `GET /api/moderation?action=pending`
2. Админ одобряет заявку → `POST /api/moderation { action: 'approve', submissionId: '...' }`
3. **Важно:** При одобрении создается ресурс в таблице `resources`
4. Функция `createResourceFromSubmission()` создает запись в таблице `resources`

### Шаг 3: Отображение
1. Ресурс появляется на главной странице при выборе соответствующей категории
2. Ресурс появляется в "Мои ресурсы" у автора ресурса
3. Ресурс доступен по прямой ссылке `/resource/{id}`

---

## Проверенные исправления

1. ✅ **Схема таблицы `resources`** - поля `subcategory_id`, `cover_image`, `author_username` теперь nullable
2. ✅ **Создание ресурса при одобрении** - функция `createResourceFromSubmission()` правильно создает ресурс
3. ✅ **Логирование** - добавлено во всех ключевых местах для отладки
4. ✅ **Передача токена** - в `MyResources.tsx` токен теперь явно передается в заголовках
5. ✅ **Обработка ошибок** - добавлена обработка ошибок во всех местах загрузки ресурсов

---

## Как проверить работу

1. **Создайте новую заявку:**
   - Перейдите на `/submit`
   - Заполните все поля и отправьте заявку

2. **Одобрите заявку:**
   - Войдите как админ
   - Перейдите в панель модерации (`/admin/moderation`)
   - Одобрите заявку

3. **Проверьте отображение:**
   - **Главная страница:** Выберите категорию заявки → ресурс должен появиться в списке
   - **Мои ресурсы:** Перейдите в `/my-resources` → ресурс должен появиться в списке
   - **Детальная страница:** Перейдите на `/resource/{id}` → должна отобразиться полная информация

4. **Проверьте логи:**
   - В логах Vercel должны быть записи:
     - `🔄 Creating resource from submission`
     - `✅ Resource created successfully`
     - `📋 User resources loaded` (для профиля)
     - `📤 Returning resources` (для главной страницы)

---

## Возможные проблемы и решения

### Проблема: Ресурс не появляется на главной странице
**Причины:**
- Категория не выбрана (нужно выбрать категорию для отображения ресурсов)
- Ресурс создан с неправильным `category_id`
- Ошибка при создании ресурса (проверьте логи Vercel)

**Решение:**
- Выберите категорию на главной странице
- Проверьте логи Vercel на наличие ошибок
- Проверьте, что ресурс создан в БД через `/api/resources/test`

### Проблема: Ресурс не появляется в "Мои ресурсы"
**Причины:**
- `author_id` в ресурсе не совпадает с ID текущего пользователя
- Ошибка авторизации (токен не передается)
- Ресурс не создан (проверьте логи)

**Решение:**
- Проверьте логи Vercel на наличие ошибок
- Убедитесь, что вы авторизованы
- Проверьте логи в `getUserResources` - там должно быть видно, какой `userId` используется

### Проблема: Ресурс не создается при одобрении
**Причины:**
- Ошибка в функции `createResourceFromSubmission`
- Неправильные данные в заявке (отсутствуют обязательные поля)
- Проблемы с БД (неправильная схема таблицы)

**Решение:**
- Проверьте логи Vercel - должны быть записи о создании ресурса
- Проверьте, что все обязательные поля заполнены в заявке
- Убедитесь, что таблица `resources` создана правильно

---

## API Endpoints

### Получение ресурсов
- `GET /api/resources?category={id}` - все ресурсы категории (для главной страницы)
- `GET /api/resources?id={id}` - конкретный ресурс (для детальной страницы)
- `GET /api/users/me/resources` - ресурсы текущего пользователя (для "Мои ресурсы")

### Модерация
- `GET /api/moderation?action=pending` - заявки на модерацию
- `POST /api/moderation { action: 'approve', submissionId: '...' }` - одобрить заявку
- `POST /api/moderation { action: 'reject', submissionId: '...' }` - отклонить заявку

### Тестирование
- `GET /api/resources/test` - все ресурсы в БД (для проверки)


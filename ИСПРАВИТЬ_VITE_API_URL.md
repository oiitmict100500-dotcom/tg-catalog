# ⚠️ ВАЖНО: Исправьте VITE_API_URL

## Проблема

В консоли браузера видно:
```
🔧 API Base URL configured: tg-catalog-one.vercel.app
```

Это **неправильно**! `VITE_API_URL` должен указывать на **BACKEND сервер**, а не на frontend!

## Решение

### 1. Разверните Backend (если еще не развернут)

Backend должен быть на отдельном хостинге:
- **Railway**: https://railway.app (рекомендуется)
- **Render**: https://render.com
- **Heroku**: https://heroku.com

### 2. Исправьте VITE_API_URL в Vercel

1. Откройте **Vercel Dashboard** → ваш проект → **Settings** → **Environment Variables**
2. Найдите переменную `VITE_API_URL`
3. Измените значение на URL вашего **BACKEND сервера**:
   - ❌ **Неправильно**: `tg-catalog-one.vercel.app` (это frontend!)
   - ✅ **Правильно**: `https://tg-catalog-backend.railway.app` (пример для Railway)
   - ✅ **Правильно**: `https://tg-catalog-api.render.com` (пример для Render)

**Важно**: 
- URL должен начинаться с `https://` (или `http://` для локальной разработки)
- URL должен быть **БЕЗ** слеша в конце
- Это должен быть URL **BACKEND**, а не frontend!

### 3. Пересоберите проект

После изменения переменной окружения:
- Сделайте новый commit (любое изменение)
- Или в Vercel Dashboard → Deployments → нажмите "Redeploy"

### 4. Проверьте

После пересборки:
1. Откройте сайт
2. Нажмите F12 (консоль браузера)
3. Должно быть:
   ```
   🔧 API Base URL configured: https://ваш-backend-url.com
   ```
   (не `tg-catalog-one.vercel.app`!)

## Примеры правильных значений

### Если backend на Railway:
```
VITE_API_URL=https://tg-catalog-backend.railway.app
```

### Если backend на Render:
```
VITE_API_URL=https://tg-catalog-api.onrender.com
```

### Если backend на Heroku:
```
VITE_API_URL=https://tg-catalog-backend.herokuapp.com
```

## Если backend еще не развернут

См. файл `БЫСТРАЯ_НАСТРОЙКА.md` для инструкций по развертыванию backend.

## Проверка работы Backend

Перед настройкой VITE_API_URL убедитесь, что backend работает:
1. Откройте URL backend в браузере: `https://ваш-backend-url.com/api/categories`
2. Должен вернуться JSON с категориями
3. Если возвращается ошибка или 404, backend не настроен правильно


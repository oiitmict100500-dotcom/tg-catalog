# Диагностика проблемы с авторизацией

## Что проверить:

### 1. Проверьте консоль браузера при клике на кнопку Telegram

Откройте консоль (F12) и нажмите на кнопку Telegram. Должны появиться логи:

```
Telegram auth callback received: {id: ..., username: ..., hasHash: true}
📤 Sending auth request to: /api/auth/telegram
📥 Auth response status: 200 (или ошибка)
```

### 2. Проверьте переменные окружения в Vercel

1. Vercel Dashboard → Settings → Environment Variables
2. Убедитесь, что есть переменная `TELEGRAM_BOT_TOKEN`
3. Значение должно быть вашим токеном из @BotFather

### 3. Проверьте домен в BotFather

1. Telegram → @BotFather
2. `/setdomain`
3. Выберите `tg_catalog_bot`
4. Домен должен быть: `tg-catalog-git-main-di7gms-projects.vercel.app`
   ИЛИ основной домен: `oiitmict100500-dotcom-tg-catalog.vercel.app`

**Важно:** Домен должен быть БЕЗ `https://` и БЕЗ слеша в конце!

### 4. Проверьте API endpoint

Откройте в браузере:
```
https://tg-catalog-git-main-di7gms-projects.vercel.app/api/categories
```

Должен вернуться JSON с категориями. Если работает, значит API настроен правильно.

### 5. Попробуйте основной домен

Если используете preview домен, попробуйте основной:
```
https://oiitmict100500-dotcom-tg-catalog.vercel.app
```

И укажите этот домен в BotFather.

## Возможные ошибки:

- **"Bot domain invalid"** - домен не настроен в BotFather
- **"Telegram bot token не настроен"** - нет переменной TELEGRAM_BOT_TOKEN в Vercel
- **"Неверные данные авторизации Telegram"** - проблема с проверкой hash (неправильный токен)
- **404 на /api/auth/telegram** - API endpoint не найден



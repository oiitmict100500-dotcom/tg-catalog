# Исправление текущих ошибок

## Проблема 1: VITE_API_URL указывает на frontend домен

**Ошибка в консоли:**
```
❌ ОШИБКА: VITE_API_URL указывает на frontend домен!
📖 Текущее значение: tg-catalog-one.vercel.app
```

### Решение:

1. Откройте проект frontend в Vercel
2. Перейдите в **Settings → Environment Variables**
3. Найдите переменную `VITE_API_URL`
4. **Удалите** текущее значение (если там `tg-catalog-one.vercel.app`)
5. Установите правильный URL вашего **backend** сервера

### Где найти URL вашего backend?

Если backend развернут на:
- **Railway**: Откройте проект → Settings → Domains → скопируйте URL
- **Render**: Откройте проект → Settings → скопируйте URL
- **Heroku**: Откройте проект → Settings → Domains → скопируйте URL
- **Vercel**: Если backend тоже на Vercel, скопируйте URL backend проекта

### Пример:

Если backend на Railway и URL: `https://tg-catalog-backend.railway.app`

Тогда в Vercel установите:
- **Name:** `VITE_API_URL`
- **Value:** `https://tg-catalog-backend.railway.app` (БЕЗ слеша в конце!)

6. Сохраните и перезапустите deployment

---

## Проблема 2: Telegram bot token не настроен

**Ошибка в консоли:**
```
❌ Auth error response: {message: 'Telegram bot token не настроен'}
📥 Auth response status: 500
```

### Решение:

Нужно добавить токен бота в переменные окружения **backend** сервера.

### Если backend на Railway:

1. Откройте проект backend в Railway
2. Перейдите в **Variables**
3. Добавьте переменную:
   - **Key:** `TELEGRAM_BOT_TOKEN`
   - **Value:** `8527710832:AAHZdTNESfu2ZAMSTD0YEb9BhTTfQb0mCfM`
4. Сохраните
5. Railway автоматически перезапустит deployment

### Если backend на Render:

1. Откройте проект backend в Render
2. Перейдите в **Environment**
3. Добавьте переменную:
   - **Key:** `TELEGRAM_BOT_TOKEN`
   - **Value:** `8527710832:AAHZdTNESfu2ZAMSTD0YEb9BhTTfQb0mCfM`
4. Сохраните
5. Render автоматически перезапустит deployment

### Если backend на Heroku:

1. Откройте проект backend в Heroku
2. Перейдите в **Settings → Config Vars**
3. Нажмите **Reveal Config Vars**
4. Добавьте:
   - **Key:** `TELEGRAM_BOT_TOKEN`
   - **Value:** `8527710832:AAHZdTNESfu2ZAMSTD0YEb9BhTTfQb0mCfM`
5. Сохраните

### Если backend на Vercel:

1. Откройте проект backend в Vercel
2. Перейдите в **Settings → Environment Variables**
3. Добавьте переменную:
   - **Name:** `TELEGRAM_BOT_TOKEN`
   - **Value:** `8527710832:AAHZdTNESfu2ZAMSTD0YEb9BhTTfQb0mCfM`
   - **Environment:** Production, Preview, Development
4. Сохраните и перезапустите deployment

---

## Проверка после исправления

1. Откройте сайт: https://tg-catalog-one.vercel.app
2. Откройте консоль браузера (F12)
3. Проверьте, что нет ошибки про VITE_API_URL
4. Нажмите "Войти через Telegram"
5. После авторизации не должно быть ошибки 500

---

## Если backend еще не развернут

Если у вас нет развернутого backend сервера, нужно сначала развернуть его:

### Варианты развертывания:

1. **Railway** (рекомендуется):
   - Зарегистрируйтесь на https://railway.app
   - Создайте новый проект
   - Подключите репозиторий с backend
   - Railway автоматически развернет backend
   - Скопируйте URL проекта

2. **Render**:
   - Зарегистрируйтесь на https://render.com
   - Создайте новый Web Service
   - Подключите репозиторий с backend
   - Render развернет backend
   - Скопируйте URL сервиса

3. **Heroku**:
   - Зарегистрируйтесь на https://heroku.com
   - Создайте новое приложение
   - Подключите репозиторий
   - Разверните backend
   - Скопируйте URL приложения

После развертывания backend:
1. Добавьте токен бота в переменные окружения backend
2. Установите VITE_API_URL в frontend на URL backend
3. Перезапустите оба deployment


# 🚨 Экстренная диагностика белого экрана

## Шаг 1: Откройте консоль браузера

1. Откройте сайт
2. Нажмите **F12** (или правый клик → "Просмотреть код")
3. Перейдите на вкладку **Console**

## Шаг 2: Проверьте логи

### ✅ Если видите:
```
🚀 Starting app initialization...
✅ App rendered successfully
```
→ Приложение загрузилось, но возможно есть ошибки в компонентах

### ❌ Если видите ошибки:

#### Ошибка: "Cannot find module" или "Failed to resolve"
→ Проблема с импортами. Проверьте, что все файлы существуют.

#### Ошибка: "Unexpected token" или синтаксическая ошибка
→ Проблема в коде. Проверьте файл, указанный в ошибке.

#### Ошибка: "Network Error" или "Failed to fetch"
→ Проблема с API. Проверьте VITE_API_URL.

#### Ошибка: "o.find is not a function" или "forEach is not a function"
→ API вернул не массив. Это должно быть исправлено в последней версии.

## Шаг 3: Проверьте вкладку Network

1. Откройте вкладку **Network** в консоли
2. Обновите страницу (F5)
3. Проверьте файлы:
   - `index.html` - должен быть статус 200
   - `index-*.js` - JavaScript файл должен загрузиться (200)
   - `index-*.css` - CSS файл должен загрузиться (200)

Если файлы не загружаются (404):
→ Проблема с путями или сборкой на Vercel

## Шаг 4: Проверьте вкладку Sources

1. Откройте вкладку **Sources**
2. Найдите файл `main.tsx` или `App.tsx`
3. Проверьте, нет ли красных подчеркиваний (ошибки)

## Шаг 5: Быстрое исправление

### Если ничего не помогает:

1. **Очистите кеш браузера**:
   - Ctrl+Shift+Delete (Windows) или Cmd+Shift+Delete (Mac)
   - Выберите "Кешированные изображения и файлы"
   - Нажмите "Очистить данные"

2. **Проверьте сборку на Vercel**:
   - Откройте Vercel Dashboard
   - Проверьте последний deployment
   - Убедитесь, что сборка прошла успешно
   - Проверьте логи сборки

3. **Проверьте переменные окружения**:
   - Vercel Dashboard → Settings → Environment Variables
   - Убедитесь, что VITE_API_URL установлен правильно
   - Должен быть URL backend, а не frontend!

## Шаг 6: Что делать, если все еще белый экран

1. **Скопируйте все ошибки из консоли** (красные сообщения)
2. **Скопируйте логи из Vercel** (Build Logs)
3. **Проверьте Network вкладку** - какие файлы не загружаются?

## Временное решение

Если нужно срочно показать сайт, можно временно:

1. Создать простой `index.html` в `frontend/public/`:
```html
<!DOCTYPE html>
<html>
<head>
  <title>TG Catalog</title>
</head>
<body>
  <h1>Сайт временно недоступен</h1>
  <p>Проверьте консоль браузера (F12) для диагностики</p>
</body>
</html>
```

Но это не решит проблему, только скроет ее.

## Контакты для помощи

Если проблема не решена:
1. Скопируйте все ошибки из консоли
2. Скопируйте скриншот Network вкладки
3. Скопируйте логи из Vercel


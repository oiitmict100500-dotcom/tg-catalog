# Загрузка проекта на GitHub

## 📋 Подготовка

### 1. Убедитесь, что .gitignore настроен правильно

Файл `.gitignore` уже настроен и исключает:
- ✅ `node_modules/` - зависимости
- ✅ `.env` файлы - секретные данные
- ✅ `dist/`, `build/` - скомпилированные файлы
- ✅ `logs/*.log` - логи
- ✅ Временные файлы

---

## 🚀 Шаги для загрузки на GitHub

### Шаг 1: Создайте репозиторий на GitHub

1. Откройте https://github.com
2. Войдите в свой аккаунт
3. Нажмите кнопку **"+"** в правом верхнем углу
4. Выберите **"New repository"**
5. Заполните:
   - **Repository name**: `tg-catalog` (или другое имя)
   - **Description**: `Каталог Telegram-ресурсов с интеграцией Telegram-бота`
   - **Visibility**: Public или Private (на ваше усмотрение)
   - ❌ **НЕ** ставьте галочки на "Add a README file", "Add .gitignore", "Choose a license" (у нас уже есть эти файлы)
6. Нажмите **"Create repository"**

---

### Шаг 2: Выполните команды в терминале

Откройте PowerShell в папке проекта и выполните:

```powershell
# Перейдите в папку проекта (если еще не там)
cd "D:\USER\Desktop\tg catalog"

# Проверьте статус
git status

# Добавьте все файлы
git add .

# Создайте коммит
git commit -m "Initial commit: TG Catalog project"

# Добавьте remote репозиторий (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/tg-catalog.git

# Загрузите код на GitHub
git branch -M main
git push -u origin main
```

---

### Шаг 3: Если возникнут проблемы

#### Проблема: "remote origin already exists"
```powershell
git remote remove origin
git remote add origin https://github.com/YOUR_USERNAME/tg-catalog.git
```

#### Проблема: "Authentication failed"
1. Используйте Personal Access Token вместо пароля
2. Или настройте SSH ключи

#### Проблема: "Large files"
Если есть большие файлы, используйте Git LFS:
```powershell
git lfs install
git lfs track "*.log"
git add .gitattributes
```

---

## 🔐 Важно: Безопасность

### ✅ Что НЕ попадет в репозиторий (благодаря .gitignore):
- `.env` файлы с токенами
- `node_modules/` 
- Логи
- Скомпилированные файлы

### ⚠️ Что нужно проверить перед загрузкой:

1. **Убедитесь, что нет .env файлов в репозитории:**
   ```powershell
   git ls-files | findstr ".env"
   ```
   Если команда ничего не выводит - всё хорошо!

2. **Проверьте, что токены не захардкожены в коде:**
   - Ищите в коде строки типа: `BOT_TOKEN=`, `TELEGRAM_BOT_TOKEN=`
   - Убедитесь, что они читаются из переменных окружения

---

## 📝 Альтернативный способ (через GitHub Desktop)

Если не хотите использовать командную строку:

1. Скачайте [GitHub Desktop](https://desktop.github.com/)
2. Установите и войдите в аккаунт
3. File → Add Local Repository
4. Выберите папку `D:\USER\Desktop\tg catalog`
5. Нажмите "Publish repository"
6. Выберите имя и видимость
7. Нажмите "Publish repository"

---

## 🎯 После загрузки

После успешной загрузки:

1. ✅ Репозиторий будет доступен по адресу: `https://github.com/YOUR_USERNAME/tg-catalog`
2. ✅ Можно добавить описание, теги, issues
3. ✅ Можно настроить GitHub Actions для автоматической сборки
4. ✅ Можно добавить collaborators

---

## 📋 Чеклист перед загрузкой:

- [ ] Проверено, что `.env` файлы не попадут в репозиторий
- [ ] Проверено, что токены не захардкожены в коде
- [ ] Создан репозиторий на GitHub
- [ ] Выполнены команды git add, commit, push
- [ ] Код успешно загружен на GitHub

---

Готово! 🚀
